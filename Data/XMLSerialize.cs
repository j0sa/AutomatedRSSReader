using System;
using System.Collections.Generic;
using System.Text;
using System.IO;
using System.Xml.Serialization;
using System.Net.Http.Headers;

namespace Data
{
    class XMLSerialize
    {
        //public void Serialize(Object podcast)
        //{
        //    XmlSerializer xmlSerializer = new XmlSerializer(podcast.GetType());
        //    using (FileStream outFile = new FileStream("PodcastData.xml", FileMode.Create, FileAccess.Write))
        //    {
        //        xmlSerializer.Serialize(outFile, podcast);
        //    }
        //}
        //public void Serialize(List<Object> podcastList)
        //{
        //    XmlSerializer xmlSerializer = new XmlSerializer(podcastList.GetType());
        //    using (FileStream outFile = new FileStream("PodcastListData.xml", FileMode.Append, FileMode.Write))
        //    {
        //        xmlSerializer.Serialize(outFile, podcastList);
        //    }
        //}
        //public void Deserialize()
        //{
        //    Object podcastObjToBeReturned;
        //    XmlSerializer xmlSerializer = new XmlSerializer(typeof(Object));
        //    using (FileStream inFile = new FileStream("PocastData.xml", ))
        //}
    }
}
